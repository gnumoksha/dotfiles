#!/usr/bin/env bash

# Search a process then kill it

fkill() {
	local pid
	pid=$(ps -ef | sed 1d | fzf -m | awk '{print $2}')

	if [ "$pid" != "" ]; then
		echo "$pid" | xargs kill -"${1:-9}"
	fi
}

fkill "$@"
