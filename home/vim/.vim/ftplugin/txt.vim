
" It can be useful to do a word wrap without line breaks only when editing
" text (not programming code), using LaTeX for example. I've added these lines
" in my .vimrc to reach this:
set wrap linebreak nolist textwidth=0 wrapmargin=0

